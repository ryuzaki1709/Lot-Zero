import React from 'react';

/**
 * Minimal static background container compliant with high-craft Linear / Stripe minimalist design system.
 * Replaces distracting canvas particle loops with calm, deep slate styling.
 */
export function KineticBackground() {
  return null;
}
